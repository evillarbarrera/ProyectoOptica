<%= form_with(model: user_store, local: true) do |form| %>
  <% if user_store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_store.errors.count, "error") %> prohibited this user_store from being saved:</h2>

      <ul>
      <% user_store.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :store_id %>
    <%= form.text_field :store_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
